pipeline {
    agent any
    stages {
        stage('docker build') {
            steps {
                sh 'docker build -f aws/Dockerfile-playwright -t myplaywright .'
                sh 'docker build -f aws/Dockerfile-aws-cli myawscli .'
            }
        }
    }
}